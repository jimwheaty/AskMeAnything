{
  "name": "askmeanything",
  "version": "0.0.1",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "prebuild": "cd ./backend && npm install && cd ../frontend && npm install",
    "build": "cd ./backend && npm build && cd ../frontend && npm build",
    "postbuild": "cd ./backend && npm start:prod && cd ../frontend && npm start"
  }
}
