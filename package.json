{
  "name": "askmeanything",
  "version": "0.0.1",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "prebuild": "cd ./backend && npm install && cd ../frontend && npm install",
    "build": "cd ./backend && nest build && cd ../frontend && react-scripts build",
    "start": "cd ./backend && nest start && cd ../frontend && react-scripts start"
  }
}
